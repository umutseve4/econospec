name: divergence

# The determinism workflow proved the bit for bit failure is real: adf-001
# params.const.estimate returns -0.010046320349888796 where the manifest holds
# -0.010046320349888798. Pinning the linear algebra backend to one thread did
# not stop it, so the threading hypothesis is dead and this workflow replaces
# argument with a controlled experiment.
#
# Each job runs the same single reference computation in a few hundred separate
# processes and fingerprints every intermediate along the way. Two arms differ
# in one variable only, the thread count, so the effect of pinning is measured
# rather than asserted. The pinned arm is the only one allowed to fail the build.

on:
  workflow_dispatch:
    inputs:
      probes:
        description: 'probes per job'
        required: false
        default: '300'
  repository_dispatch:
    types: [divergence]
  pull_request:
    paths:
      - '.github/workflows/divergence.yml'
      - 'tools/divergence_probe.py'
      - 'tools/divergence_report.py'
      - 'econospec/**'

permissions:
  contents: read

jobs:
  probe:
    name: ${{ matrix.mode }} arm, batch ${{ matrix.batch }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        mode: [pinned, unpinned]
        batch: [1, 2, 3]
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Record the machine
        run: |
          set -u
          {
            grep -m1 'model name' /proc/cpuinfo | sed 's/^/cpu    /'
            nproc | sed 's/^/cores  /'
            python -c "import numpy; print('numpy  ' + numpy.__version__)"
          } > /tmp/machine.txt
          cat /tmp/machine.txt

      - name: One probe first, with its traceback carried out of the log
        run: |
          set -u
          set +e
          python tools/divergence_probe.py . > /tmp/first.json 2> /tmp/first.err
          code=$?
          set -e
          cat /tmp/first.json || true
          cat /tmp/first.err || true
          if [ "$code" -ne 0 ]; then
            body=$(tail -n 40 /tmp/first.err)
            body=${body//'%'/'%25'}
            body=${body//$'\n'/'%0A'}
            body=${body//$'\r'/''}
            echo "::error title=Probe refused to run (exit $code)::$body"
            exit 1
          fi

      - name: Take the sample
        env:
          PROBES: ${{ github.event.inputs.probes || '300' }}
        run: |
          set -u
          if [ "${{ matrix.mode }}" = "unpinned" ]; then
            # setdefault in econospec/__init__.py leaves an explicit value alone,
            # so exporting here is what makes this arm the control.
            export OMP_NUM_THREADS=4
            export OPENBLAS_NUM_THREADS=4
            export MKL_NUM_THREADS=4
            export NUMEXPR_NUM_THREADS=4
            export VECLIB_MAXIMUM_THREADS=4
          fi
          : > /tmp/probe.ndjson
          i=1
          errors=0
          while [ "$i" -le "$PROBES" ]; do
            set +e
            python tools/divergence_probe.py . >> /tmp/probe.ndjson 2>> /tmp/probe.err
            [ $? -ne 0 ] && errors=$((errors + 1))
            set -e
            i=$((i + 1))
          done
          echo "lines $(wc -l < /tmp/probe.ndjson), failed probes $errors"
          if [ "$errors" -ne 0 ]; then
            body=$(tail -n 30 /tmp/probe.err)
            body=${body//'%'/'%25'}
            body=${body//$'\n'/'%0A'}
            echo "::error title=${errors} probes of ${PROBES} failed to run::$body"
          fi

      - name: Report what the sample shows
        run: |
          set -u
          set +e
          python tools/divergence_report.py /tmp/probe.ndjson > /tmp/report.txt 2>&1
          diverged=$?
          set -e
          cat /tmp/report.txt
          {
            echo "### ${{ matrix.mode }} arm, batch ${{ matrix.batch }}"
            echo ''
            echo '```'
            cat /tmp/machine.txt
            echo ''
            cat /tmp/report.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          body=$(cat /tmp/machine.txt /tmp/report.txt)
          body=${body//'%'/'%25'}
          body=${body//$'\n'/'%0A'}
          body=${body//$'\r'/''}
          echo "::error title=${{ matrix.mode }} arm batch ${{ matrix.batch }} (exit $diverged)::$body"
          if [ "$diverged" -eq 2 ]; then
            echo "the probe produced nothing to read"
            exit 1
          fi
          if [ "${{ matrix.mode }}" = "pinned" ] && [ "$diverged" -ne 0 ]; then
            echo "the pinned arm still diverges, so the pinning is not the answer"
            exit 1
          fi
          echo "arm finished with divergence exit code $diverged"
