# Pinned so that a green run can be reproduced later.
# The reference kernel needs only numpy. Everything else exists so that an
# independent implementation can disagree with it.
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
statsmodels==0.14.4
linearmodels==6.1
pytest==8.3.4
